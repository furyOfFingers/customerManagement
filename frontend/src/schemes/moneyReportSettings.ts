export const schemeMoneyReportSettings = {
  required: "${label} is required!",
  value: {
    range: "${label} must be between ${min} and ${max}",
  },
  label: {
    range: "${label} must be between ${min} and ${max}",
  },
  hint: {
    range: "${label} must be between ${min} and ${max}",
  },
  teacher_salary: {
    range: "${label} must be between ${min} and ${max}",
  },
};
