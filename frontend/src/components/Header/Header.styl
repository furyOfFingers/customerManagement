.container
  display flex
  height 50px
  position: fixed;
  width: 100%;
  padding 10px
  padding-left 20px
  border-bottom 2px solid #ccc

.containerShadow
  box-shadow: 1px 6px 6px 2px rgba(204,204,204,0.58);
  -webkit-box-shadow: 1px 6px 6px 2px rgba(204,204,204,0.58);
  -moz-box-shadow: 1px 6px 6px 2px rgba(204,204,204,0.58);

.spin
  position absolute
  top 10px
  right 50%

.avatar
  margin 0 10px

.username
  display flex
  align-items center